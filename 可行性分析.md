# 可行性分析

## 结论

在当前 A800 服务器和 VNC 条件下，完成“文本指令驱动、双虚拟相机观测、MoveIt 规划、MuJoCo Panda 执行、抓取放置与复检”的**仿真最小闭环**可行性高。

但它不是把现有三个原型直接串起来即可完成：统一 MuJoCo 场景、MoveIt 场景同步、抓取附着语义和视觉闭环是主要工程工作。真实相机与真实机械臂迁移不包含在本阶段可行性结论中。

## 已验证依据

| 能力 | 当前证据 | 对可行性的意义 |
| --- | --- | --- |
| MuJoCo 图像渲染 | 已在独立环境渲染 RGB-D 杯子场景 | 固定/腕部虚拟相机可在同类机制上实现 |
| VNC 图形显示 | 已能从 VNC 打开相机帧和 RViz | 可以观察全局场景、相机和规划轨迹 |
| MoveIt 规划 | 红杯任务的预抓取、接近、抬升、放置预位均已由 OMPL 分段规划 | 传统规划可作为低层运动生成器 |
| MuJoCo 轨迹执行 | 已按关节状态同步回放 MoveIt 轨迹，并以各关节保守限速保证跟踪 | MoveIt 输出可稳定作用于统一 MuJoCo Panda，而非 Fake Panda |
| 抓取与放置 | 已完成夹爪开闭、MuJoCo 运动学附着、释放和托盘区域复检 | 证明阶段 2 真值任务基线，不代表视觉能力 |
| 双相机 viewer 模式 | 旧项目已有全局 viewer 叠加固定/腕部实时相机的实现 | VNC 观测交互可独立复现到本实验目录 |

这些证据已覆盖阶段 0–2 的真值任务基线，以及阶段 3 的首个红色圆柱 CAD 点云配准抓放回合。该阶段使用规则语义而不是 VLM，且只支持解析圆柱 OBJ；阶段 4 的 VLM 语义控制、通用 CAD 网格、多颜色和随机化尚未实现，不能把当前结果称为通用端到端视觉任务成功。

## 分项可行性

| 分项 | 可行性 | 主要条件与风险 |
| --- | --- | --- |
| 统一 Panda + 杯子 + 托盘 MuJoCo 场景 | 高 | 需要在本目录拥有 Panda 模型和场景资产，并统一关节、相机和物体命名 |
| 全局 + 固定 + 腕部 VNC 视图 | 高 | 采用独立渲染节奏，viewer 不得阻塞物理循环 |
| MuJoCo 与 MoveIt 轨迹联动 | 中高 | 必须同步起始关节状态、轨迹时间戳和关节限位；不能再让 Fake Panda 与 MuJoCo Panda 双执行 |
| 碰撞场景同步 | 中高 | 桌子、托盘、杯子位置在每段技能规划前由同一快照更新 |
| 稳定抓取放置 | 中 | 纯接触动力学对杯子、夹爪摩擦和时间步敏感；第一版采用“满足接触与夹爪条件后附着”的混合抓取模型更可靠 |
| RGB-D 几何定位 | 高 | 彩色杯子、固定视角和深度条件下可先用颜色语义与深度几何；需记录误差且不使用真值控制 |
| VLM 语义选择与复检 | 中高 | A800 足以运行小到中型本地 VLM；模型输出须限制为低频结构化语义，不能负责 6D 位姿或关节控制 |
| 泛化到任意物体/真实机器人 | 中低 | 需要大量视觉随机化、相机标定、抓取模型和真实安全系统；不应作为本阶段验收目标 |

## 性能与运行方式

物理仿真、MoveIt、VLM 与 viewer 需要异步运行：

```text
MuJoCo 物理：固定控制步长
相机/几何：按观测帧率更新
MoveIt：仅在进入新技能时规划
VLM：按任务阶段异步调用，允许秒级延迟
VNC viewer：15–30 FPS 左右的显示，不参与控制时钟
```

A800 的 GPU 主要用于后续 VLM 推理；MuJoCo 物理、MoveIt 规划和状态机主要依赖 CPU。VLM 的实际模型、显存占用和单帧延迟须在阶段 4 单独基准测试，不能现在预设具体数值。

## 关键风险与化解

1. **仿真世界漂移**：以本目录场景配置为唯一来源，并对 MuJoCo/MoveIt 做关节和物体快照同步；桌面碰撞物只覆盖基座前方作业面，避免与 Panda 固定底座产生伪碰撞。
2. **抓住但 MoveIt 仍把杯子当障碍物**：当前阶段 2/3 在接近时临时移除目标杯，MuJoCo 使用运动学附着；抓取/释放时同步为 MoveIt AttachedCollisionObject 仍是下一阶段必须补齐的工作。
3. **VLM 幻觉或超时**：JSON 校验、置信度门限、超时取消和颜色语义基线回退。
4. **VNC 卡顿影响任务**：viewer 放在独立显示循环；关闭 viewer 后任务结果应不变。
5. **同机 ROS 串话**：每个实验会话显式设置独立 `ROS_DOMAIN_ID` 和日志目录。
6. **把真值当视觉能力**：真值只进入评测日志和阶段 2 单元测试，阶段 3 起由代码检查禁止进入控制路径。

## 推荐决策

按 [实施路线.md](实施路线.md) 推进是合理的：先完成阶段 1 的统一场景与双相机 VNC 可视化，再完成阶段 2 的真值任务基线，之后才将 RGB-D 视觉和 VLM 接入。此顺序能把模型、几何、规划、抓取物理和执行问题分开定位，避免 VLM 成为所有失败的笼统解释。
