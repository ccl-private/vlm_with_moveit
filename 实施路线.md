# 实施路线与变更门禁

本文规定执行顺序。每完成一个阶段必须运行该阶段验收、更新运行说明和记录已知限制，再进入下一阶段。

## 已完成：阶段 0

- 原生 RoboStack Jazzy + MoveIt 环境位于本目录；
- Panda 假控制器可由 MoveIt 规划和执行；
- RViz 可在 VNC 中观察轨迹；
- MuJoCo RGB-D 与颜色语义基线可单独运行。

这些是基础设施，不是端到端任务成功。

## 已完成：阶段 1

1. 新建统一 MuJoCo Panda 抓取场景及其专属配置；不修改现有彩色杯子相机原型。
2. 将场景配置设为 MuJoCo 与 MoveIt 碰撞场景的唯一来源，定义带时间戳的关节/物体同步快照。
3. 输出固定和腕部 RGB-D、相机标定、Panda/夹爪关节状态和仅评测真值。
4. 提供 VNC 可见的 MuJoCo 全局执行视图，并在同一 viewer 实时叠加与感知同源的固定/腕部相机画面；保留 RViz 作为规划观察窗口，且显示不能阻塞控制。
5. 验收坐标系：固定相机估计点转换至 `panda_link0` 后，与评测真值误差可计算；每个启动会话使用显式独立 ROS 域。

已实现统一 Panda/桌面/三杯/托盘场景、三路相机、带时间戳的关节与碰撞物同步快照，以及离屏坐标系验收。固定相机的三只杯子可见性、深度有效性和相机外参方向已自动验证通过；VNC 中已手工确认实时叠加窗口、独立腕部窗口以及 `D`（深度）、`R`（重置）交互。

## 已完成：阶段 2（真值任务基线）

1. 已实现 MuJoCo Panda 执行适配器；Fake 控制器只保留给 MoveIt 服务，不执行任务轨迹；
2. 每段技能规划前已同步 MuJoCo 关节状态，桌面、托盘与非目标杯子进入 MoveIt 碰撞场景；
3. 共享执行器已替换历史运动学附着：只有双指真实接触确认后才启用 MuJoCo weld 抓持约束，随后完成接近、闭爪、抬升、放置预位、物理松爪与托盘复检；
4. 已提供一键 VNC 观察入口和 JSON 回合证据；该通路只作真值基线，不得作为视觉闭环成绩。

已知限制：阶段 2 仍是读取物体真值的单元测试，不是视觉闭环；其运输段没有重新发布附着物碰撞体。阶段 3 已在运输/放置规划前发布由视觉 CAD 位姿推导的 `AttachedCollisionObject`，并以物理接触和 weld 执行夹持。

## 已完成：阶段 3（已登记 CAD 的视觉抓放）

1. 实现与未来真实 VLM 契约一致的规则语义适配器：文本输出 `task`、`target_query`、
   `destination_query` 和置信度，不输出坐标；
2. 建立本目录对象目录，预载每个待操作物体的 OBJ/STL/PLY、尺度、对称性和
   `T_object_grasp` 抓取模板；
3. 实现颜色阈值实例分割器，输入 RGB 与 `target_query`，输出带时间戳的目标 mask；
4. 从 mask、深度和 `T_panda_link0_camera_fixed` 生成点云，以预载 CAD 表面匹配得到
   `T_panda_link0_object`、残差、内点率和对称性标记；控制路径禁止调用
   `evaluation_only_truth`，真值仅写入误差日志；
5. 首先用红色圆柱 OBJ 完成“CAD 匹配 → 顶抓模板 → MoveIt → 放置”回合；通用网格后续采用
   模型表面采样/特征粗配准与 ICP/GICP 精配准；
6. 用红、绿、蓝杯各完成回合，保存语义 JSON、mask、深度点云摘要、CAD 配准结果、真值误差、
   规划与执行事件。

上述链路已在红、绿、蓝圆柱，紫色方块、洋红色长方体和黄色带把手马克杯上完成自动端到端回合：日志记录模型 ID、
掩码、`T_panda_link0_object`、表面残差、内点率、对称性、抓取模板、`AttachedCollisionObject`、MoveIt
事件及仅评测的托盘成功标志。此处的“完成”仅限对象目录中登记的专用几何；通用任意网格配准仍未实现。

## 已完成：阶段 3 性能收敛（正常速度档）

现有基线为稳定性逐点回放 MoveIt 轨迹：每个离散点最低执行 0.35 秒，造成首个 CAD 回合的
纯运动时间约为 43 秒。这不是规划、CAD 匹配或 GPU 性能瓶颈，不能作为正常交互速度保留。

1. 执行器必须消费整条 MoveIt `JointTrajectory` 的 `time_from_start`，以连续插值控制 MuJoCo，
   不能再将每个规划点单独停顿回放；
2. 新增“正常速度档”：保持每关节速度上限、跟踪误差和抓取稳定性门限，但去除固定的 0.35 秒
   点间下限；参数必须集中、可记录，不能散落在任务逻辑中；
3. VNC 以固定视觉刷新率观察最新仿真状态，不得通过 `sleep` 成为控制环的节拍器；无 VNC 与
   VNC 的仿真轨迹和成功判定必须等价；
4. 启动 MoveIt 后应允许服务常驻复用；固定 ROS 发现等待只计入冷启动，不能计入一次任务的
   感知到动作延迟；
5. 红色圆柱的纯运动时间（首个夹爪打开到释放后稳定前）目标不超过 12 秒，VNC 画面目标为
   约 30 FPS；仍须满足 MoveIt 规划成功、最大关节跟踪误差不超过 0.08 rad、附着和托盘评测成功。

已验证：MoveIt 原始四段轨迹合计为 11.80 秒，正常档连续执行合计约 8.99 秒；从首个夹爪打开到
托盘复检的纯运动时间为 9.84 秒，最大跟踪误差为 0.0345 rad、最大终点误差为 0.0107 rad，CAD
回合成功。VNC 同回合记录 303 帧，等效约 30.8 FPS；关闭 VNC 的回合保持同一执行时长与成功判定。

## 已完成：阶段 3 真实物理放置

此前的 `release_to_tray` 会在松爪时直接重写杯子的 MuJoCo `freejoint` 坐标到托盘中心。这是为了
早期稳定性留下的伪放置，不能计入真实抓放成功。修复必须严格执行以下顺序：

1. 从 CAD 对象高度、抓取模板和静态托盘几何推导托盘中心上方的夹爪预放置位与释放位；不得读取
   MuJoCo 托盘或杯子真值；
2. 夹爪在左右指尖均已接触目标后建立 MuJoCo 抓持约束，依次由 MoveIt 规划至托盘正上方、竖直下降至释放位；
3. 在当前物理位姿打开夹爪并仅解除附着，禁止重写杯子的 `qpos`、`qvel`、位置或朝向；
4. 由 MuJoCo 接触动力学自然落稳，再使用独立的托盘区域评测；日志必须记录释放前后位姿、
   自由下落时间和是否发生任何状态重置；
5. 验收时 VNC 中须可见杯子始终随夹爪到托盘上方，松开后自然下落；代码审查应能证明放置路径
   未调用任何杯子坐标写入函数。

已验证：原托盘中心 `(0.74, -0.20, 0.45)` 对 Panda 的下向释放姿态没有有效 IK，故工位托盘迁至
不与三杯重叠、且经规划验证可达的 `(0.52, 0.25, 0.45)`。CAD 回合先到托盘上方 `z=0.750`，再
下降至由目录推导的夹爪释放位 `z=0.644`。最新 VNC 回合在 `z≈0.542` 松爪，杯子无坐标重置地自然
降至 `z≈0.532` 并停在托盘中；仿真纯运动为 11.55 秒、VNC 墙钟运动为 16.82 秒、显示约 21 FPS。

该速度档仅针对仿真演示。迁移真机前必须根据负载、碰撞距离、急停和安全规范重新标定速度、
加速度及力矩限制，禁止直接复用此处参数。

## 已完成：圆柱目标颜色切换

1. 红、绿、蓝三个场景圆柱映射到同一个 `cylindrical_cup_v1` OBJ 和顶抓模板，颜色只负责语义筛选和实例 ID 映射；
2. 执行链路由“文本颜色 → 颜色分割 → 目录实例 → 场景对象 ID”动态选择 `red_cup`、`green_cup` 或 `blue_cup`；
3. MoveIt 常驻服务在新回合发布场景时会删除当前目标遗留的碰撞体，避免切换颜色后把待抓杯仍视为障碍物；
4. 三杯初始位与托盘及彼此分离；蓝色阈值额外排除蓝灰色地面；
5. 已在同一个常驻 MoveIt 服务中连续完成红→绿→蓝三回合，每回合均有对应对象的附着、物理松爪和通过的托盘复检。

## 已完成：语义选模与多几何 CAD 接入

1. 将对象目录拆为可复用的几何模型和独立的颜色/场景实例；VLM 输出必须携带 `target_model_id`，目录负责一致性校验；
2. 语义成功后继续以颜色阈值生成目标 mask；该分割接口保留给未来 SAM 或小型实例分割模型替换；
3. 将圆柱专用匹配器抽为按 `geometry_type` 分派的匹配器，并加入紫色正方体作为首个 `box` 验收对象；
4. 紫色正方体已完成 RGB-D → `box_cube_v1` 位姿 → 顶抓模板 → MoveIt → MuJoCo 物理放置；随后在同一常驻 MoveIt 服务中切回红色圆柱，两回合均通过托盘复检。

这证明的是“不同已登记基础几何的选模和执行链路”，不是通用任意网格 6D 配准：当前 `box` 匹配器只
适用于桌面水平、具有四重竖直轴对称的正方体。复杂 CAD 的下一步是引入模型特征粗配准与 ICP 精配准，
同时保留本节的目录校验、分割和抓取模板接口。

## 已完成：通用 CAD 抓取标注与姿态变换

抓取逻辑不能按圆柱、正方体或长方体分别硬编码。每个 CAD 模型应在同目录维护一个版本化的抓取标注
文件，标注项定义 CAD 坐标系下的抓取框、一对相对可接触面（ID、法向、中心）、夹爪闭合轴、接近距离、
抬升距离、开度、质量分及对称等价姿态。运行时会拒绝“两个面不相对”或“面法向不平行于闭合轴”的标注。
运行时统一执行：

```text
T_base_object（视觉 CAD 配准） × T_object_grasp（CAD 标注）
→ 候选 T_base_grasp
→ MoveIt IK / 碰撞 / 路径筛选
→ 最优可执行抓取框
→ 夹爪接触、开度和相对位姿验证
```

洋红色、桌面 yaw 非零的长方体只作为首个验收对象：其 CAD 标注要求夹爪横向与长边平行，系统必须将
匹配到的对象 yaw 传入每一段 MoveIt 末端姿态。之后接入新 CAD 时只新增网格、目录实例和抓取标注，
不新增“某形状专用”的控制分支。

洋红色长方体已完成 RGB-D 分割、CAD yaw 配准、标注抓取框变换、五段 MoveIt 规划与 MuJoCo 物理
放置。初始位置在抬升阶段暴露 Panda 工作空间边界，移至可达且与其它物体分离的位置后通过托盘复检。

## 已完成：物理夹持与夹爪朝向修复

旧版“附着”会在每个物理步直接改写物体 freejoint，因此物体只是视觉上跟随夹爪，不能作为夹持成功。
现已删除该路径，改为以下原子门槛：

1. 闭爪后，MuJoCo 必须同时报告左、右 finger pad 与目标的接触；任一侧缺失即拒绝附着；
2. CAD 抓取标注的 `jaw_closing_axis_object` 经 `T_base_object` 变换后，必须与实际 hand 横向闭合轴满足 `|dot| >= 0.95`；这保证两指沿被标注两接触面的法向相向闭合；
3. 仅在上述条件满足时，以当前手掌—物体相对锚点和相对四元数启用 MuJoCo weld。它是接触确认后的稳定夹持模型，不再写入物体 `qpos` 或 `qvel`；
4. `OPEN_GRIPPER` 解除 weld 后完全交给接触动力学稳定，日志明确记录两项状态重置均为 `false`。

针对洋红色、非零 yaw 的长方体已完成无 VNC 自动回归：左右接触数均为 12，闭合轴对齐为
`0.999998`；对象在抬升中由 `z=0.421` 随夹爪升至 `z=0.615`，释放点为
`[0.517, 0.255, 0.494]`，随后自然稳定在托盘内。该结果证明物体在松爪前确实由已验证的夹爪约束搬运。

后续视觉复核发现，仅有“双侧接触数”仍可能是手指压在物体顶面。抓取标注现升级到版本 2：每个平行夹爪
抓取框都必须给出 `opposing_contact_pair`（正、负两片面的 ID、法向与中心），运行时还将 MuJoCo 接触点
转换回 CAD 坐标系并验证左右手指分别命中这两面。该检查曾正确拒绝过长方体的顶面接触；修正真实张开行程
为每侧 40 mm、把抓取框降到侧面中部后，左右各 9 个接触且 `opposing_contact_pair=passed`，完整回合再次成功。

## 待执行：通用 CAD 网格接入

成对夹持面标注、MoveIt 候选筛选和 MuJoCo 接触校验已不依赖具体形状；下一阶段要补齐任意复杂网格的
视觉与仿真资产链路，顺序如下：

1. 新 CAD 导入时统一米制、对象坐标系、视觉网格与 MuJoCo 碰撞网格；
2. 在相邻 `.grasp.json` 中标注一个或多个 `opposing_contact_pair`，先用加载器拒绝几何关系非法的标注；
3. 从 RGB-D mask 取得点云，以特征全局配准给出粗位姿，再以 ICP/GICP 精配准和置信度门限决定是否执行；
4. 对每个可用抓取框执行 MoveIt 筛选、双指相对面接触校验、抬升和物理放置回归；
5. 对遮挡、对称歧义、薄壁/易碎面和抓取失败记录归因，必要时重观测或换用下一抓取框。

### 通用匹配器 TODO

- [x] 新增 `geometry_type=mesh` 的 `GenericMeshCadMatcher` 兜底：对 OBJ 三角面采样，以 RGB-D 点云到网格表面的修剪 Chamfer 距离搜索桌面 yaw 与平移，并在残差/内点率不足时拒绝规划；圆柱、方块和带把手马克杯仍保留专用高可靠匹配器。
- [ ] 将该兜底从“桌面竖直物体的 yaw + 平移”扩展为任意 6D 初始姿态搜索，避免把它误称为完整通用配准。
- [ ] 为 OBJ/STL/PLY 增加统一的网格预处理：米制校验、对象坐标系校验、表面点采样、法向计算、尺度和版本记录。
- [ ] 实现粗配准：从 RGB-D mask 点云移除桌面和离群点，使用 FPFH/RANSAC、Super4PCS 或等价方法生成多个初始位姿候选。
- [ ] 实现 ICP/GICP 精配准，优先验证 point-to-plane ICP；记录残差、内点率、有效点覆盖率和收敛状态。
- [ ] 增加匹配置信度门限：粗配准失败、ICP 局部最优、残差过大或覆盖率不足时拒绝规划。
- [ ] 增加对称性处理：保存等价姿态候选，不强行选择唯一 yaw；将候选姿态交给 MoveIt 做可达性和碰撞筛选。
- [ ] 将 CAD 模型可见表面、把手/孔洞等关键区域纳入分区评分，避免只对齐杯身而忽略真正抓取区域。
- [ ] 增加遮挡与重观测策略：固定相机和腕部相机之间切换，目标出框、关键区域不可见或置信度下降时重新观测。
- [ ] 为每个通用 CAD 抓取模板补齐 `opposing_contact_pair`，并完成 MoveIt 规划、双指接触、抬升、物理松爪和托盘复检回归。
- [ ] 增加可重复测试场景：随机位置、yaw、光照、深度噪声、遮挡和相机延迟，并分别统计粗配准、ICP、抓取和放置成功率。

完成前不得宣称“任意 CAD 已端到端支持”；目前已完成的是任意 CAD 可复用的抓取标注和执行验证接口。

## 已完成：非对称马克杯把手的水平夹取

本验收不以“杯身侧夹”作为替代。红、绿、蓝对象已统一称为圆柱体；真正的马克杯使用带把手的视觉网格和独立碰撞网格，位于 Panda 已验证工作空间内。文本指令“抓取黄色马克杯把手并放到托盘”会输出 `mug_with_handle_v1` 与 `grasp_region=handle`。

马克杯 CAD 包含 `handle_horizontal_pinch_v1`：夹爪横向闭合轴平行于把手厚度方向，成对接触面为
把手外侧竖杆的内、外两面；杯身、杯口、把手孔洞和未标注把手区域均不会作为接触面。RGB-D 匹配从把手的
非对称凸出方向恢复唯一 yaw，抓取框随该 yaw 变换，再由 MoveIt 筛选姿态与路径。验收日志证明：

1. 语义目标为马克杯把手而非圆柱体；
2. 估计的 yaw 可观测，且实际夹爪闭合轴与把手成对面法向对齐；
3. 左右指尖接触点分别命中把手内、外两面；

已完成首个闭环验收：黄色 `mug_with_handle_v1` 的文本语义输出 `grasp_region="handle"`，RGB-D/CAD 匹配输出唯一把手 yaw；夹爪在闭合过程中探测到双侧接触后立刻锁定。日志记录左右接触数 6/4、闭合轴对齐度约 0.99994 和 `opposing_contact_pair="passed"`。随后携物抬升、移送、下降、物理松爪，最终托盘评测为成功。圆柱对象统一更名为 `cylindrical_object_v1`，不再称为马克杯。

马克杯和托盘均直接置于桌面：杯身中心为 `z=0.445 m`，托盘底板中心为 `z=0.407 m`。把手 yaw 仅由 RGB-D 中可见的非对称网格表面估计，不使用额外绿色视觉标记。规划失败时终端会输出目标位姿及 MoveIt 服务末尾诊断。物体已在抬升后于托盘上方物理松爪、无状态重置地稳定在托盘内。

## 待执行：阶段 4

1. 将 VLM 权重与依赖安装到本目录；
2. 用真实 VLM 适配器替换阶段 3 规则语义适配器，保持 `target_query` JSON 完全不变；
3. 对 VLM 输出做结构化校验、置信度拒绝和失败归因；
4. 运行多颜色、多初始位姿的回合评测。

## 不允许的捷径

- 不从父项目读取模型、代码或场景来绕过本目录边界；
- 不把 MuJoCo 物体真值直接交给视觉控制路径；
- 不把手工 `/vision_moveit/target_pose` 调试结果计入任务成功；
- 不将仿真执行开关用于真实机械臂。
