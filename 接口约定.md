# 视觉闭环接口约定

本文是后续实现的模块契约。字段可以增加，以下职责、坐标系和真值隔离规则不得改变。

## 坐标系与单位

| 坐标系 | 含义 |
| --- | --- |
| `panda_link0` | Panda 底座，也是 MoveIt 目标的统一参考系 |
| `camera_fixed` | MuJoCo 固定 RGB-D 相机光学坐标系 |
| `camera_wrist` | 固连于 `panda_hand` 的腕部 RGB-D 相机光学坐标系 |
| `panda_hand` | 夹爪末端坐标系 |
| `object/<id>` | 由感知估计的杯子或托盘坐标系 |

位姿为右手系、米和四元数；每条跨模块消息必须有时间戳、`frame_id`、回合编号和置信度。相机外参 `T_panda_link0_camera_fixed` 只能由场景标定配置提供，禁止由真值物体位姿反推。

MuJoCo 原始相机坐标的图像右方为 `+X`、图像上方为 `+Y`、视线方向为 `-Z`；深度反投影必须使用 `(x, -y, -z)` 这一约定，再乘 `T_panda_link0_camera`。阶段 1 快照同时保存 `world_from_camera` 和 `base_from_camera`，正式几何模块只使用后者。

## 正式数据流

| 阶段 | 输入 | 输出 | 拒绝条件 |
| --- | --- | --- | --- |
| 相机 | MuJoCo 场景 | 固定/腕部 RGB、深度、内参、外参、时间戳 | 帧丢失或深度无效 |
| VLM/语义 | 文本、RGB、任务上下文 | 目标类别/实例、任务意图、语义置信度 | 输出不符合结构、低置信度或与指令矛盾 |
| 几何感知 | RGB-D、标定、语义目标 | `panda_link0` 下 6D 位姿、协方差、抓取候选 | 位姿超工作空间、深度不足或不确定性过大 |
| 状态机 | 感知估计、执行/接触事件、历史状态 | 下一技能与目标位姿、重试或终止 | 非法状态迁移或超过重试次数 |
| MoveIt | 技能目标、当前关节状态、碰撞物 | 分段关节轨迹或失败原因 | 碰撞、IK 不可达或规划超时 |
| MuJoCo 执行 | 关节轨迹、夹爪开闭 | 执行完成、关节状态、接触/附着/放置事件 | 超时、轨迹跟踪异常或对象未附着 |
| 复检 | 最新 RGB-D、任务状态 | 成功、重观测、重试或失败归因 | 目标不在托盘或出现错误对象 |

## 阶段 3/4：可替换语义、分割与 6D 几何接口

语义理解、实例分割和几何定位必须是三个独立模块。真实 VLM 只替换语义理解（或未来的
语义分割实现），不得直接产生基座坐标、末端位姿或关节命令。

### 语义任务结果

无论当前规则适配器还是真实 VLM，均必须输出以下等价 JSON。阶段 3 的假实现由文本中的
颜色词生成；阶段 4 的真实 VLM 读取文本、RGB 与任务上下文后也只能产生同一契约。

```json
{
  "task": "pick_and_place",
  "target_query": {
    "category": "cup",
    "attributes": {"color": "red"}
  },
  "target_model_id": "cylindrical_cup_v1",
  "destination_query": {"category": "tray"},
  "confidence": 0.98,
  "source": "rule_baseline"
}
```

字段 `source` 在阶段 4 可为模型名；`target_query` 是供分割器查询的语义条件，`target_model_id`
是 VLM 选择的 CAD 几何模型 ID，而不是物体坐标。对象目录必须验证“类别、属性、模型 ID”三者
能唯一解析为一个场景实例；不匹配时拒绝执行。缺少类别、目标属性冲突、模型 ID 不存在、置信度
低于阈值或任务类型不支持时，状态机必须拒绝执行。

### 实例分割结果

颜色阈值分割器是阶段 3 的可替换基线：输入 RGB 与 `target_query`，输出目标的二值掩码，
不读取 MuJoCo 物体真值。未来可替换为 VLM 分割、检测器加分割器或其他模型，但输出契约不变。

```text
SegmentationResult = {
  frame_id, timestamp, target_query, mask[H,W], bounding_box_xyxy,
  confidence, source
}
```

掩码面积过小、与图像边界大面积相交、存在多个不可区分候选，或置信度不足时，不能进入
几何计算与 MoveIt。

### RGB-D 物体位姿结果

几何模块输入 `SegmentationResult`、同时间戳深度图和相机内外参，输出基座系位姿：

```text
PoseEstimate = {
  frame_id: "panda_link0",
  timestamp,
  position_m: [x, y, z],
  orientation_xyzw: [qx, qy, qz, qw],
  cylinder_axis_base: [ax, ay, az],
  axial_yaw_observable: false,
  position_covariance_3x3,
  axis_covariance_3x3,
  segmentation_confidence,
  geometry_confidence
}
```

`orientation_xyzw` 是供抓取生成器使用的规范化抓取方向；只有在物体旋转完整可观测时，才能
把它视为真实完整 6D 朝向。圆柱杯无纹理且轴对称时，深度只能可靠得到位置与圆柱轴方向，
绕圆柱轴的 yaw 不可观测，必须以 `axial_yaw_observable: false` 明确标记，禁止伪造高置信度。

### 从深度到圆柱位姿

1. 从 mask 内有效深度像素用相机内参反投影为相机点云；MuJoCo 相机须先按 `(x, -y, -z)`
   光学轴约定转换。
2. 用标定外参 `T_panda_link0_camera` 变换到 `panda_link0`。
3. 使用 RANSAC 移除桌面平面和离群点；依据杯子目录中的半径、高度范围拟合圆柱，而不是把
   前表面深度中位数误当成圆柱中心。
4. 拟合圆柱中心得到 `xyz`，主轴得到 `roll/pitch`；对于轴对称杯，抓取生成器按圆柱轴合成
   夹爪朝下的规范 yaw。对带把手杯、长方体等 yaw 重要物体，应从纹理、边缘、CAD 匹配或
   固定/腕部双视角融合获得 yaw。
5. 点数、拟合残差、深度覆盖率、协方差和工作空间检查共同决定 `geometry_confidence`；任一
   阈值不满足时只允许重观测或失败，不允许规划。

阶段 3 的控制通路只允许消费上述 `PoseEstimate`。`evaluation_only_truth(...)` 只能生成评测
误差和标注，代码审查与自动测试均应确保它不被语义、分割、几何、抓取生成或控制模块调用。

## 已知 CAD 模型的 6D 配准与抓取契约

阶段 3 的主路线是“预载对象 CAD 模型 + RGB-D 点云配准”，基本几何拟合只作为没有 CAD 时的
降级方案。对象目录分为“几何模型”和“场景实例”：几何模型登记模型 ID、OBJ/STL/PLY 网格、
单位、尺度、对称性、可抓取/禁止接触部位和一个或多个对象坐标系下的抓取模板
`T_object_grasp`；场景实例登记类别、颜色等语义属性、MuJoCo 对象 ID，并引用一个几何模型。
因此同一 CAD 可以被多个颜色实例复用，颜色不会写入 CAD 网格。

```text
观测点云 P_base + 模型表面 M_object
→ 求 T_panda_link0_object
→ T_panda_link0_grasp = T_panda_link0_object × T_object_grasp
→ MoveIt 可达性、碰撞与预抓取路径筛选
```

模型匹配必须保存粗配准候选、精配准结果、内点率、表面残差和对称等价姿态。单视角遮挡或
对称性导致多个可行姿态时，输出候选集合而不是随意选择唯一姿态；腕部相机重观测可用于消歧。

首个红色圆柱 CAD 基线读取预载 OBJ 中的网格尺度，以圆柱封闭表面的有符号距离作鲁棒代价，
在初值邻域搜索 `T_panda_link0_object`。圆柱轴与轴向 yaw 的对称性仍须显式记录；抓取模板使用
顶部垂直接近。后续任意 CAD 网格应升级为“模型表面采样/特征全局配准 → ICP/GICP 精配准”的
通用实现，不能把圆柱解析匹配冒充为通用网格匹配。

### 几何匹配器分派

几何模型必须声明 `geometry_type`。运行时先由 VLM 输出的 `target_model_id` 经目录校验解析模型，
再按 `geometry_type` 分派匹配器；语义模块和执行器不得依赖“红杯”等硬编码对象名。

```text
VLM: target_query + target_model_id
→ ObjectCatalog 校验并解析场景实例
→ RGB-D 阈值/未来 SAM 产生该实例 mask
→ geometry_type: cylinder | box | future_mesh
→ 对应 CAD 匹配器输出 PoseEstimate
→ 模板抓取、MoveIt、MuJoCo
```

首批 `box` 只验证与桌面平行放置的正方体：由 RGB-D 点云匹配立方体 CAD 的中心和垂直轴，绕竖直轴
按四重旋转对称处理。它证明非圆柱 CAD 的模型选择、匹配器分派和独立抓取模板均已连通；任意朝向
复杂网格仍须使用模型表面采样、全局特征配准与 ICP/GICP。

## 技能状态机

首个任务只允许以下顺序，所有状态都必须写入日志：

```text
OBSERVE
→ PLAN_PREGRASP → EXECUTE_PREGRASP
→ PLAN_APPROACH → EXECUTE_APPROACH
→ CLOSE_GRIPPER → VERIFY_GRASP
→ PLAN_LIFT → EXECUTE_LIFT
→ PLAN_PLACE → EXECUTE_PLACE
→ OPEN_GRIPPER → VERIFY_PLACE
→ SUCCEEDED | RETRY | FAILED
```

`VERIFY_GRASP` 至少依据夹爪接触/附着事件和物体相对末端位姿；`VERIFY_PLACE` 至少依据托盘区域事件与相机复检。不能仅以“MoveIt 轨迹执行返回成功”作为抓取或放置成功。

## ROS 边界

| 话题 | 类型 | 用途 |
| --- | --- | --- |
| `/vision_moveit/target_pose` | `geometry_msgs/PoseStamped` | 单段 MoveIt 规划输入；仅由状态机发布，手工发布只作调试 |
| `/vision_moveit/planned_trajectory` | `moveit_msgs/RobotTrajectory` | 规划轨迹记录与可视化 |
| `/vision_moveit/planning_status` | `std_msgs/String` | 规划/执行状态，不作为唯一任务结果 |
| `/vision_moveit/task_status` | 后续定义的结构化状态 | 当前回合、状态机阶段、失败原因和重试次数 |
| `/vision_moveit/sim_events` | 后续定义的结构化事件 | 接触、夹取附着、释放、托盘放置与异常 |

早期可先在 Python 进程内传递相机帧和结构化对象；只有跨进程/跨语言边界时才固定为 ROS 消息。不得为了 ROS 化而让真值绕过感知模块。

## 真值隔离与执行开关

真值字段统一以 `evaluation_only_` 或 `sim/ground_truth` 命名，只能由评测器读取。控制路径只可使用相机、标定、关节状态、接触事件和感知估计。

模拟执行开关必须命名为 `execute_in_simulation`，默认 `false`。它不等同于未来真实机械臂的执行授权。

## 场景同步与抓取原子性

每个技能规划前，MuJoCo 执行适配器必须向 MoveIt 同步同一个带时间戳的快照：Panda 关节状态、桌面、托盘和全部可碰撞杯子位姿。MoveIt 返回的轨迹只能作用于该快照对应的回合；状态发生变化时必须重新规划。

当 `VERIFY_GRASP` 判定夹取成功时，同一状态转换必须完成两件事：在 MuJoCo 中建立稳定的物体附着/抓取状态；在 MoveIt 中把该杯子设为 `panda_hand` 的附着碰撞物。`OPEN_GRIPPER` 时反向同步。任一动作失败均不得进入下一技能。

运行时把物理、VLM、规划与 VNC 渲染解耦。显示帧没有按时到达不得阻塞物理或轨迹执行；VNC 视图没有控制权限。
