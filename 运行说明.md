# 阶段 0 至阶段 3 运行说明

本文件说明基础设施、统一 MuJoCo 场景、阶段 2 真值抓放基线，以及阶段 3 的 CAD 视觉抓放回合。阶段 2 使用物体真值作为单元测试输入；阶段 3 使用模拟 RGB-D 点云与预载 OBJ 配准，但规则语义适配器仍不是真实 VLM。完整路线以 [实施路线.md](实施路线.md) 为准。

## 阶段 1：统一场景与 VNC 相机观察

先在无界面模式做自动验收；它不会启动 ROS、MoveIt 或机械臂任务：

```bash
cd /slow_disk/ccl/codes/Lerobot-Mujoco/moveit_experiment
./scripts/verify_unified_scene.sh
./scripts/preview_unified_scene.sh --headless
```

通过后会在 `logs/unified_preview/` 写入三路 RGB 首帧、`stage1_snapshot.json`（同一时刻的相机标定、9 个关节和 4 个碰撞对象快照）与 `verification.json`。后两个文件中仅验收字段使用的物体真值不属于控制输入。

从 VNC 桌面终端运行实时观察窗口：

```bash
cd /slow_disk/ccl/codes/Lerobot-Mujoco/moveit_experiment
./scripts/preview_unified_scene.sh
```

需要保存预览视频时，在同一条命令追加独立的输出目录：

```bash
./scripts/preview_unified_scene.sh --video-dir logs/videos/scene_preview
```

退出窗口后目录中会生成 `main.mp4`（主视图）和 `wrist.mp4`（腕部相机）。录像仅适用于 VNC 实时预览；`--headless` 只写三路首帧和场景快照，不录制视频。

主窗口是 MuJoCo 全局视图，右上为固定 RGB-D 相机；另一个标题为“腕部 RGB-D 相机”的窗口显示腕部画面。Xvnc/VirtualGL 不稳定地在同一窗口传输相隔较远的像素区域，因此采用两个同源 GPU 窗口。预览复用感知链创建的渲染器并且每帧只取固定、腕部两路 RGB；这避免额外离屏上下文导致的黑屏。按主窗口的 `D` 才将两幅画面切为深度伪彩，按 `R` 重置场景，按 `Esc` 退出。腕相机位于夹爪横向外侧并向抓取区前倾：待机时红杯在中心可见区、机械臂仅位于边缘；窗口只用于观察，不启动控制，也不影响物理时钟。

在本服务器上，VNC 的原生 OpenGL 为软件 `llvmpipe`。预览脚本默认使用已验证的 VirtualGL EGL 后端，将 MuJoCo 的 OpenGL 绘制重定向到 A800 的 `egl0`，再将画面传回 VNC。若需指定另一张 GPU，可在启动前设置 `MOVEIT_VGL_DEVICE=egl1`、`egl2` 或 `egl3`；只作图形兼容性诊断时才设置 `MOVEIT_USE_VIRTUALGL=0` 回退软件渲染。

当前实现使用统一 MuJoCo Panda 场景：规则语义适配器选择已登记的 CAD 目标，颜色实例分割和 RGB-D 点云完成几何配准，再由 CAD 抓取标注、MoveIt 和 MuJoCo 物理执行完成抓放。规则适配器仅用于替代未来 VLM 的结构化语义输出；物体 6D 位姿始终由独立几何模块从 RGB-D 获取。

MoveIt 规划桥已实现为 C++ 节点。运行无界面 Panda 与 MoveIt：

```bash
./scripts/build.sh
./scripts/start_moveit_headless.sh
```

它订阅 `/vision_moveit/target_pose`，仅接受 `panda_link0` 或 `base_link` 下的位姿；规划成功后发布 `/vision_moveit/planned_trajectory`。离线视觉演示尚未自动发布这些目标；阶段 2 的专用真值基线会通过这些话题请求规划，并由 MuJoCo 执行器回放轨迹。

启动文件会在控制器管理器就绪后自动轮询并激活假控制器。这样规避 RoboStack/Jazzy 中控制器服务“先返回、后完成加载”的时序问题；正常日志应出现“`Panda 模拟控制器均已激活`”。关闭时按 `Ctrl+C`，不会修改仓库外的 ROS 配置或环境。

激活脚本默认使用 ROS 域 `87`，用于隔离这套实验与同服务器上的其他 ROS 程序。若需要接入既有 ROS 系统，可在启动前显式设置 `ROS_DOMAIN_ID` 覆盖它。

## 在 VNC 中观察 Panda 与规划轨迹

请从 VNC 桌面内的终端启动：

```bash
cd /slow_disk/ccl/codes/Lerobot-Mujoco/moveit_experiment
./scripts/start_moveit_rviz.sh
```

RViz 会显示 Panda、碰撞场景和 `/display_planned_path` 轨迹。待终端出现“`Panda 模拟控制器均已激活`”后，在第二个 VNC 终端发布一个测试目标：

```bash
cd /slow_disk/ccl/codes/Lerobot-Mujoco/moveit_experiment
source scripts/activate.sh
ros2 topic pub --once /vision_moveit/target_pose geometry_msgs/msg/PoseStamped \
"{header: {frame_id: panda_link0}, pose: {position: {x: 0.45, y: 0.0, z: 0.45}, orientation: {x: 1.0, y: 0.0, z: 0.0, w: 0.0}}}"
```

规划成功后，RViz 会先显示紫色半透明规划轨迹，随后 Panda 假控制器会执行该轨迹，实体模型的姿态会移动到目标位置。`start_moveit_rviz.sh` 只连接本目录启动的模拟 Panda；桥接节点的 `execute_in_simulation` 默认值为关闭，接入真实机械臂时不会因该代码自动执行。关闭时在第一个终端按 `Ctrl+C`。

## 阶段 2：在 VNC 中验收一次完整抓放

从 VNC 桌面终端运行以下一条命令：

```bash
cd /slow_disk/ccl/codes/Lerobot-Mujoco/moveit_experiment
./scripts/run_truth_vnc.sh
```

脚本会在独立的 ROS 域 `72` 启动仅负责规划的 MoveIt 服务，并在 A800/VirtualGL 上打开两个窗口：主窗口显示 Panda 与右上固定 RGB-D 相机，独立窗口显示腕部 RGB-D 相机。主窗口状态依次应为 `MoveIt: pregrasp`、`approach`、`lift with red cup`、`place with red cup` 和 `Gripper: release into tray`；红杯最后应位于黄色托盘内。任务成功后两个窗口会停在终态，按主窗口 `Esc` 关闭。

终端会输出回合 JSON，同时写入 `logs/episodes/truth_baseline_latest/episode.json`。验收时确认：`success` 为 `true`，事件包含 `grasp_attached`、`grasp_released`，并且最后的 `tray_verification.result` 为 `passed`。VNC 仅用于观察；关闭窗口会安全停止本次回合。

无 VNC 的自动复测可运行：

```bash
cd /slow_disk/ccl/codes/Lerobot-Mujoco/moveit_experiment
ROS_DOMAIN_ID=72 ./scripts/start_moveit_headless.sh
# 在另一个终端：
ROS_DOMAIN_ID=72 ./scripts/run_truth_baseline.sh --instruction '抓取红色杯子并放到托盘'
```

阶段 2 的目标杯在接近阶段被暂时从 MoveIt 世界碰撞物中移除，允许夹爪建立接触；其它杯子、托盘和基座前方的桌面仍参与规划。阶段 3 已不再使用“每步改物体坐标”的运动学保持：只有左右 finger pad 都接触目标且 CAD 闭合轴与夹爪横向轴对齐后，才启用 MuJoCo 的相对抓持约束；松爪时只解除约束，不写入物体状态。完整 AttachedCollisionObject 同步仍是后续真机化工作。

## 阶段 3：语义选模的 CAD 点云配准与抓放

这一回合验证的控制链为：文本指令 → 规则语义 JSON（含 `target_model_id`）→ 对象目录交叉校验颜色、类别和 CAD → 固定 RGB-D 相机的颜色实例掩码 → 深度反投影为 `panda_link0` 点云 → 按 `geometry_type` 分派的 OBJ 表面配准 → CAD 相邻 `.grasp.json` 的抓取标注变换 → MoveIt 分段规划与 MuJoCo 执行。红、绿、蓝圆柱复用 `cylindrical_object_v1`；紫色正方体使用 `box_cube_v1`；洋红色长方体使用 `rectangular_block_v1` 并验证 CAD yaw 会传到手腕姿态。颜色只决定视觉分割的实例查询和场景对象 ID，CAD 网格不携带颜色。颜色阈值分割器是当前可替换实现，未来可在同一输入/输出接口换成 SAM 或小型实例分割模型。控制代码不会读取目标物的 MuJoCo 真值位姿；真值仅在释放后作为独立评测写入日志。闭爪过程在首次检测到左右指尖同时接触时立即锁定，再验证 CAD 标注闭合轴和成对接触面；放置阶段夹爪实际到达托盘正上方、下降并在当前物理位置松爪，禁止将物体瞬移到托盘。

从 VNC 桌面终端运行：

```bash
cd /slow_disk/ccl/codes/Lerobot-Mujoco/moveit_experiment
./scripts/run_cad_matching_vnc.sh --instruction '抓取绿色杯子并放到托盘'
# 用不同 CAD 几何进行端到端验证：
./scripts/run_cad_matching_vnc.sh --instruction '抓取紫色方块并放到托盘'
# 验证 CAD 标注随物体 yaw 旋转的抓取姿态：
./scripts/run_cad_matching_vnc.sh --instruction '抓取洋红色长方体并放到托盘'
```

脚本省略参数时默认抓红杯；当前接受三色圆柱杯、黄色马克杯、紫色方块和洋红色长方体指令。每次一键 VNC 回合默认随机分配一个 `20`–`219` 的独立 ROS 域；若需复现或连接外部 ROS 工具，可先设置 `MOVEIT_ROS_DOMAIN_ID` 固定它。脚本通过 A800 的 VirtualGL 打开主视图与腕部 RGB-D 视图。主窗口会依次显示 `CAD match`、`pregrasp`、`approach`、`lift`、`place_above`、`place_descend` 和物理松爪。夹爪在预抓取前张开、到达抓取位后闭合、到释放位再次张开；每次开/闭均保留 0.8 秒物理稳定时间，便于从 VNC 确认两片手指从目标的相对两侧夹住它。目标物必须随夹爪到托盘上方，在下降后自然落入托盘；成功后停在终态，按主窗口 `Esc` 安全退出。

### 阶段 3 常用参数、录像与日志

一键 VNC 脚本会把以下任务参数原样传给 CAD 回合，并自动补上 `--vnc`、启动/关闭同域 MoveIt 服务。录像应使用一个不会被下一次回合覆盖的目录，例如：

```bash
cd /slow_disk/ccl/codes/Lerobot-Mujoco/moveit_experiment
MOVEIT_ROS_DOMAIN_ID=106 ./scripts/run_cad_matching_vnc.sh \
  --instruction '抓取黄色马克杯把手并放到托盘' \
  --video-dir logs/videos/mug_handle_20260923 \
  --attached-speed-scale 6.0
```

| 参数/环境变量 | 适用范围 | 作用 |
| --- | --- | --- |
| `--instruction '<指令>'` | 阶段 2、3 | 指定任务目标；阶段 3 省略时为“抓取红色杯子并放到托盘”。抓黄色马克杯须明确写“把手”。 |
| `--video-dir <目录>` | 阶段 1 VNC 预览、阶段 3 VNC 回合 | 保存 `main.mp4` 和 `wrist.mp4`。只在带窗口的 VNC 模式可用，目录会自动创建。 |
| `--attached-speed-scale <正数>` | 阶段 3 | 夹住目标之后的轨迹时长倍率，默认 `6.0`；数值越小越快，数值越大越慢且通常更稳。 |
| `--vnc` | 直接调用 `run_cad_matching.sh` 或 `run_truth_baseline.sh` | 打开两路观察窗口，必须从有 `DISPLAY` 的 VNC 桌面终端运行。一键 `*_vnc.sh` 已自动传入。 |
| `--headless` | `preview_unified_scene.sh` | 阶段 1 不创建窗口，只输出首帧和快照；不产生 MP4。 |
| `MOVEIT_ROS_DOMAIN_ID=<N>` | `run_cad_matching_vnc.sh`、`run_truth_vnc.sh` | 固定一键 VNC 回合的 ROS 域；阶段 3 默认随机分配，阶段 2 默认 `72`。 |
| `ROS_DOMAIN_ID=<N>` | 手动分终端启动的阶段 2、3 | `start_moveit_headless.sh` 与任务脚本必须使用同一个值。 |
| `MOVEIT_VGL_DEVICE=eglN` | 所有 VNC 图形脚本 | 选择 VirtualGL 的 GPU，默认 `egl0`；仅排查图形兼容性时可设 `MOVEIT_USE_VIRTUALGL=0` 使用软件渲染。 |

阶段 2 的真值基线目前没有 `--video-dir`；它可用于实时观察和回合 JSON 验收，但不录制 MP4。阶段 3 的回合 JSON 始终写入 `logs/episodes/cad_matching_latest/episode.json`，一键 VNC 的任务与 MoveIt 末尾日志分别写入 `logs/stage3_cad_vnc/task_latest.log` 和 `logs/stage3_cad_vnc/moveit_latest.log`；这些 `latest` 文件会随下一次回合更新，因此录像建议每次使用新目录。

验收日志为 `logs/episodes/cad_matching_latest/episode.json`，至少确认以下字段：

- `semantic_result.target_model_id`、`cad_model.model_id` 和目录实例一致；圆柱为 `cylindrical_cup_v1`，紫色方块为 `box_cube_v1`；
- `pose_estimate.matching_method` 与几何类型一致：圆柱为 `cad_cylinder_surface_sdf_grid_refinement`，方块为 `cad_box_surface_sdf_grid_refinement`；
- `surface_residual_m`、`inlier_ratio` 与 `geometry_confidence` 均存在，且 `axial_yaw_observable` 为 `false`；
- 事件包含 `moveit_pregrasp_complete`、`grasp_attached`、`moveit_lift_complete`、`moveit_place_above_complete`、`moveit_place_descend_complete`、`grasp_released_physical` 和 `physical_settle_complete`；
- `grasp_attached.left_contacts`、`right_contacts` 均大于零，`closing_axis_alignment >= 0.95`；对带 `opposing_contact_pair` 的 CAD，字段 `opposing_contact_pair` 必须为 `passed`；
- `physical_release.qpos_reset_at_release` 与 `qvel_reset_at_release` 均为 `false`；
- `evaluation_only_success` 为 `true`。

无 VNC 的自动复测可运行：

```bash
cd /slow_disk/ccl/codes/Lerobot-Mujoco/moveit_experiment
ROS_DOMAIN_ID=76 ./scripts/start_moveit_headless.sh
# 在另一个终端：
ROS_DOMAIN_ID=76 ./scripts/run_cad_matching.sh --instruction '抓取红色杯子并放到托盘'
```

若已按上例在第一个终端启动 MoveIt，也可从 VNC 桌面第二个终端直接运行并录像；两个终端的 ROS 域必须一致：

```bash
ROS_DOMAIN_ID=76 ./scripts/run_cad_matching.sh --vnc \
  --instruction '抓取红色杯子并放到托盘' \
  --video-dir logs/videos/red_cup_manual
```

当前对登记的圆柱网格 `cylindrical_object_v1.obj`（逻辑 CAD ID 为 `cylindrical_object_v1`）实现了表面解析配准；红、绿、蓝三个圆柱均复用它。它会明确将绕圆柱轴的 yaw 标为不可观测，抓取模板因而选择与 yaw 无关的顶部垂直接近。不要将此实现理解为通用任意网格配准；通用 OBJ/STL/PLY 仍需按 [接口约定.md](接口约定.md) 中约定，增加模型表面采样、特征粗配准和 ICP/GICP 精配准。

## 已验收：黄色马克杯把手的水平二指抓取

真正的非对称网格是 `assets/object_models/mug_with_handle_v1.obj`。其把手向 CAD 局部 `+Y` 凸出，`mug_with_handle_v1.grasp.json` 中的 `handle_horizontal_pinch_v1` 把外侧竖杆厚度两侧标为 `handle_outer_bar_positive_y` 与 `handle_outer_bar_negative_y`；这两处才是允许的二指接触面。

先启动无界面 MoveIt 服务，再执行：

```bash
ROS_DOMAIN_ID=106 ./scripts/run_cad_matching.sh --instruction '抓取黄色马克杯把手并放到托盘'
```

最新验收日志位于 `logs/episodes/cad_matching_latest/episode.json`。成功回合应同时包含：语义 `grasp_region: "handle"`、匹配方法 `mug_handle_body_fit_and_visible_mesh_yaw`、`axial_yaw_observable: true`、事件 `gripper_dual_contact`，以及 `grasp_attached.opposing_contact_pair: "passed"`。松爪后的 `physical_release` 必须声明不重写 `qpos/qvel`，并由 `evaluation_only_success: true` 确认最终落盘。

马克杯与托盘都直接置于桌面：杯身中心为 `z=0.445 m`，托盘底板中心为 `z=0.407 m`。把手方向由杯体/把手网格的 RGB-D 可见表面估计；场景不再需要会和绿色圆柱混淆的额外颜色标记。

在 VNC 桌面终端执行以下命令可查看全程：

```bash
./scripts/run_cad_matching_vnc.sh --instruction '抓取黄色马克杯把手并放到托盘'
```

若规划失败，终端会打印每个 MoveIt 请求的三维位置和四元数；VNC 脚本还会自动输出 MoveIt 服务日志末尾，其中包含 `GOAL_STATE_INVALID`、IK 或碰撞原因。

### 正常速度档

阶段 3 的默认执行模式是面向演示和算法验证的正常速度档：它连续回放 MoveIt 的整条时间轨迹，
而不是在每个离散轨迹点停顿。红杯回合的纯运动验收目标为 12 秒以内；VNC 保持平滑观测，但不会
限制轨迹的仿真时间。真实物理放置的最近一次 VNC 验收为 11.548 秒仿真纯运动、16.82 秒墙钟运动、
约 21 FPS，最大关节跟踪误差为 0.04285 rad；详细时序与误差位于回合日志的 `execution` 字段。
VNC 预览每帧按需渲染固定、腕部两路 RGB；感知模块仍使用完整 RGB-D。该速度档不等同于
真机速度配置，真机必须按负载、碰撞风险和安全规范单独限速。
