# 验证计划与成功标准

## 验收原则

每一级只验证自身边界。后一级失败不得用前一级的真值结果掩盖；“规划成功”“控制器执行成功”“物体被抓住”“任务完成”是四个不同指标。

## 分阶段验收

| 阶段 | 交付与测试 | 通过标准 |
| --- | --- | --- |
| 0：基础环境 | 独立 Jazzy/MoveIt、无界面 Panda、RViz、RGB-D 相机原型 | 已完成；MoveIt 规划与假控制器执行可复现 |
| 1：统一任务场景 | 一个 MuJoCo 场景包含 Panda、夹爪、桌子、杯子、托盘、固定和腕部相机 | VNC 可见 MuJoCo 全局视图、同源固定/腕部 RGB-D 视图与状态；相机/关节/物体坐标系一致 |
| 2：真值任务基线 | 用真值物体位姿仅作为状态机与规划单元测试输入，完整执行抓取放置 | 有接触附着、托盘放置和复检事件；结果单列为“真值任务基线” |
| 3：视觉几何闭环 | 以 RGB-D + 颜色语义基线替换真值物体输入 | 控制代码不读取物体真值；保存估计误差与端到端任务结果 |
| 4：VLM 语义闭环 | 以本地 VLM 适配器替换颜色语义适配器 | 指令目标选择、异常判定通过 JSON 校验；VLM 不输出关节控制 |
| 5：评测与重试 | 随机种子、多颜色、多初始位姿、失败重试与汇总 | 报告各阶段成功率、失败归因、耗时及视觉误差 |

阶段 1 已通过 VNC 手工验收；阶段 2 已通过首个红杯真值回合的自动验收；阶段 3 已完成首个红色圆柱 CAD 点云配准回合的自动验收。阶段 3 的规则语义适配器不是真实 VLM，且通用网格、多颜色、多初始位姿与重试尚未开始；不得夸大为通用视觉闭环。

阶段 2 已留存的自测证据为 `logs/episodes/truth_baseline_latest/episode.json`：其中 `success` 为 `true`，且事件顺序包含 `gripper_close`、`grasp_attached`、`moveit_lift_complete`、`moveit_place_complete`、`grasp_released` 与结果为 `passed` 的 `tray_verification`。规划使用桌面、托盘和非目标杯子的 MoveIt 碰撞场景；目标杯在接近阶段临时不作为世界碰撞物，以允许末端建立抓取接触。这是阶段 2 的明确混合抓取模型，阶段 3 前仍需补充 MoveIt AttachedCollisionObject 同步。

阶段 3 的附加验收：规则语义适配器必须输出与未来真实 VLM 相同的 `target_query` JSON；颜色
分割器不得读取物体真值；每回合保存 mask、有效深度比例、圆柱拟合残差、`PoseEstimate` 与仅
评测真值误差。对无纹理轴对称圆柱，日志必须声明 `axial_yaw_observable=false`，不得把不可观测
yaw 纳入高置信度 6D 指标。任一感知置信度门限未通过时，系统应拒绝 MoveIt 请求并记录原因。

圆柱 CAD 基线的附加验收：控制模块读取 OBJ 对象目录和 RGB-D 点云，且不得把
`evaluation_only_truth` 的坐标作为配准初值、抓取目标或碰撞物位置；日志必须记录模型 ID、
`T_panda_link0_object`、表面残差、内点比例、对称性和所选 `T_object_grasp`。仅评测模块可在
回合结束后计算 CAD 位姿误差。红色圆柱必须从 VNC 中可见地完成“模型匹配 → 顶抓 → 放置”。

本次自动证据写入 `logs/episodes/cad_matching_latest/episode.json`：`cad_model.model_id` 为
`red_cylindrical_cup_v1`，配准方法为 `cad_cylinder_surface_sdf_grid_refinement`，并记录了表面残差、
内点率和 `axial_yaw_observable=false`。回合完成后的 `evaluation_only_success=true` 只用于验收，
不参与感知、目标生成、碰撞物生成或 MoveIt 请求。

正常速度档的附加验收：日志必须分别记录 MoveIt 原始轨迹时长、执行器设定时长、纯运动时长、
最大关节跟踪误差与渲染帧数。红色圆柱从首个 `gripper_open` 至释放后物理稳定的纯运动时间必须
不超过 12 秒，且仍满足 CAD 配准、MoveIt 规划、`grasp_attached`、`grasp_released` 和托盘成功。
VNC 仅影响观测帧数，不得改变执行器目标、仿真时间、轨迹时长或最终成功判定；冷启动的 ROS
服务发现等待须与任务运动指标分开报告。

伪放置修复前的速度数据不再作为当前验收证据。最新真实物理放置回合的
`cad_matching_latest/episode.json` 已通过时间与成功门限：仿真纯运动 11.548 秒，五段执行器指令
时长合计约 10.29 秒，最大跟踪误差不超过 0.04285 rad，且 `evaluation_only_success=true`。

真实物理放置的附加验收：日志必须记录 `moveit_place_above_complete`、
`moveit_place_descend_complete`、`grasp_released_physical` 和 `physical_settle_complete`；释放事件前后
杯子 `qpos` 的变化只能来自 MuJoCo 物理步，不得有任意托盘中心坐标写入。释放位置的 XY 必须在
托盘内部区域，杯子由夹爪带至该位置后再松开。托盘成功仍由回合末尾的独立评测产生，不能在释放
时直接设置为成功。

最新 VNC 证据记录 `grasp_released_physical`、`physical_settle_complete` 和通过的托盘复检；
`physical_release.qpos_reset_at_release=false` 与 `qvel_reset_at_release=false`。释放杯心约为
`(0.521, 0.249, 0.542)`，稳定后约为 `(0.521, 0.248, 0.532)`，证明杯子实际自然下落而非瞬移。
VNC 墙钟运动为 16.82 秒、显示约 21 FPS；VNC 预览按需取得固定、腕部两路 RGB，感知链仍使用原始
640×480 RGB-D。两路预览复用感知渲染器，以规避 VirtualGL 的额外离屏上下文黑屏问题。

阶段 1 的额外验收：关闭或降低 VNC viewer 帧率后，仿真步数、轨迹执行结果和任务状态机结果应保持一致；固定和腕部画面均能与对应感知帧时间戳匹配。阶段 5 的额外验收：每个 ROS 会话使用独立域，且加入视觉随机化后单独报告指标。

### 三色圆柱切换验收

在同一个已启动的 MoveIt 服务中，按红→绿→蓝连续执行“抓取某颜色杯子并放到托盘”。每回合必须满足：

1. 文本语义中的颜色、CAD 目录实例、`grasp_attached.object_id` 与 `tray_verification.object_id` 一致；
2. 三个颜色条目均引用同一 OBJ 网格和同一顶抓模板；
3. 切换到上回合的非目标杯时，其遗留碰撞体已删除，预抓取规划仍成功；
4. 三回合均有 `grasp_released_physical`，且 `qpos_reset_at_release=false`、`qvel_reset_at_release=false`，托盘复检为 `passed`。

本次连续验收已通过：红、绿、蓝分别对应 `red_cup`、`green_cup`、`blue_cup`，三回合均完成真实物理释放与托盘复检。蓝色分割也验证排除了蓝灰色地面连通域。

### 语义选模与方块几何验收

新增验收从同一结构化语义接口开始，不能由控制代码根据颜色硬编码选择匹配器：

1. 指令“抓取紫色方块并放到托盘”必须输出 `target_query.category=block`、`target_query.attributes.color=purple` 与 `target_model_id=box_cube_v1`；对象目录必须将它们解析为唯一的 `purple_cube` 实例；
2. 分割器只以 `purple` 查询 RGB 图像，输出 mask 后再由深度反投影；它可以被 SAM 或小模型替换，但不得改写后续 `SegmentationResult` 契约；
3. 目录必须按 `geometry_type=box` 分派 `cad_box_surface_sdf_grid_refinement`，不得复用圆柱匹配器；
4. 方块必须完成五段 MoveIt 轨迹、`grasp_attached`、物理松爪和托盘复检，且释放时两项状态重置标志均为 `false`；
5. 在同一个常驻 MoveIt 服务中，随后执行“抓取红色杯子并放到托盘”必须重新选择 `cylindrical_cup_v1` 和 `cad_cylinder_surface_sdf_grid_refinement`，证明选模不是服务启动时固定的。

本次自动验收已通过第 1–5 项。方块回合的 `geometry_confidence=0.984`、`evaluation_only_success=true`，
回合末方块位于托盘内；切回红色圆柱后同一服务再次返回 `cylindrical_cup_v1` 并通过托盘复检。

### 通用 CAD 抓取标注验收

每个可抓取 CAD 必须有独立、版本化的抓取标注文件，而不是在控制代码中写入形状规则。标注至少含
`T_object_grasp`、接触区域、夹爪闭合轴、开度与预抓取参数。系统必须将每个标注框乘以视觉得到的
`T_base_object` 后作为 MoveIt 候选；日志记录候选数量、被选标注 ID、物体 yaw 与抓取 yaw。

洋红色长方体以非零 yaw 放在桌面，作为首个可视验收对象。其标注要求夹爪横向与长边平行；抓取、抬升、
放置五段均应使用该变换后的姿态，而不是固定向下姿态。闭爪后相对末端的对象 yaw 必须保持，最后仍以
无状态重置的物理松爪及托盘复检验收。新 CAD 的接入验收只允许增加网格、目录实例和标注文件。

本次验收已通过：洋红色长方体由 `rectangular_block_v1` 及相邻 `.grasp.json` 标注解析，RGB-D/CAD
估计中心与场景位置一致、输出非零 `object_yaw_rad`，抓取四元数随该 yaw 变化。端到端回合包含五段
MoveIt 完成事件、`grasp_attached`、无状态重置的物理松爪及通过的托盘复检。

回归同时验证了候选选择日志：`candidate_count=1`、选中 `top_down_long_side_v1`、无候选被拒绝，且
`evaluation_only_success=true`。当未来标注文件包含多个抓取框时，同一选择通路会按质量依次请求 MoveIt。

### 物理夹持与接触面朝向验收

“夹爪有闭合动画”不构成夹持成功。每个物理夹取回合还必须满足：

1. `grasp_attached.method=physical_fingertip_contact_and_weld`，并记录左右接触数均大于零；
2. `grasp_attached.closing_axis_alignment >= 0.95`。该值是 CAD 标注的接触面法向（`jaw_closing_axis_object`）变换至基座系后，与实际夹爪闭合轴的绝对点积；
3. 对平行夹爪，`grasp_attached.opposing_contact_pair` 必须为 `passed`：左、右指尖的接触点在 CAD 坐标系中必须分别位于标注的正、负相对表面附近；单纯同一顶面或同一侧面的双侧接触必须拒绝；
4. 在 `moveit_lift_complete` 遥测中，物体高度必须随夹爪显著高于接近完成时的高度，证明未在桌面或地面滑落；
5. `physical_release.qpos_reset_at_release=false` 且 `qvel_reset_at_release=false`，随后独立 `tray_verification` 为 `passed`。

最新洋红色长方体自动回归通过上述四项：左右接触各 12，闭合轴对齐 `0.999998`，物体在抬升阶段由
`z=0.421` 到 `z=0.615`，释放后稳定在托盘内。该回归同时覆盖旋转长方体：夹爪横向轴按 CAD 标注垂直
于两条长侧面，而不是固定使用世界坐标方向。

抓取框高度也属于 CAD 标注的一部分：若夹爪张开行程不足或抓取框过高，指尖会压住顶面而非夹住两侧。
现已修正为每侧实际张开 `0.040 m`，并将长方体抓取框降至侧面中部高度。最新回归中左右各有 9 个接触，
`opposing_contact_pair=passed`，对象从 `z=0.425` 抬至 `z=0.623` 后落入托盘。

## 首个回合的判定

任务“抓取红色杯子并放到托盘”仅在同时满足以下条件时成功：

1. 语义模块选择的目标是红色杯子；
2. 几何模块由 RGB-D 估计出可用的目标位姿；
3. 状态机依次完成预抓取、接近、闭夹爪、抬升、放置和释放；
4. MuJoCo 接触/附着事件确认杯子曾被夹住；
5. 杯子最终位于托盘区域，且相机复检未发现错误杯子。

## 必存日志

每个回合保存在 `logs/episodes/<episode_id>/`，至少包括：文本指令、随机种子、相机 RGB/深度、VLM 原始输出与校验结果、几何估计、仅评测真值、状态机转移、MoveIt 轨迹、夹爪命令、MuJoCo 事件和最终判定。

## 指标

| 指标 | 计算方式 |
| --- | --- |
| 语义目标正确率 | VLM/语义目标与评测标签一致的回合占比 |
| 位姿可用率 | 满足置信度和工作空间约束的感知输出占比 |
| 平移/旋转误差 | 估计与真值的误差，仅作评测 |
| 规划成功率 | 有效技能目标中 MoveIt 成功规划的比例 |
| 抓取成功率 | 发生有效夹取附着的比例 |
| 放置成功率 | 杯子最终落在目标托盘区域的比例 |
| 端到端成功率 | 同时满足首个回合五项判定的比例 |
| 重试收益 | 重试后成功率相对首次尝试的提升 |
