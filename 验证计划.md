# 验证计划与成功标准

## 验收原则

每一级只验证自身边界。后一级失败不得用前一级的真值结果掩盖；“规划成功”“控制器执行成功”“物体被抓住”“任务完成”是四个不同指标。

## 分阶段验收

| 阶段 | 交付与测试 | 通过标准 |
| --- | --- | --- |
| 0：基础环境 | 独立 Jazzy/MoveIt、无界面 Panda、RViz、RGB-D 相机原型 | 已完成；MoveIt 规划与假控制器执行可复现 |
| 1：统一任务场景 | 一个 MuJoCo 场景包含 Panda、夹爪、桌子、杯子、托盘、固定和腕部相机 | VNC 可见 MuJoCo 全局视图、同源固定/腕部 RGB-D 视图与状态；相机/关节/物体坐标系一致 |
| 2：真值任务基线 | 用真值物体位姿仅作为状态机与规划单元测试输入，完整执行抓取放置 | 有接触附着、托盘放置和复检事件；结果单列为“真值任务基线” |
| 3：视觉几何闭环 | 以 RGB-D + 颜色语义基线替换真值物体输入 | 控制代码不读取物体真值；保存估计误差与端到端任务结果 |
| 4：VLM 语义闭环 | 以本地 VLM 适配器替换颜色语义适配器 | 指令目标选择、异常判定通过 JSON 校验；VLM 不输出关节控制 |
| 5：评测与重试 | 随机种子、多颜色、多初始位姿、失败重试与汇总 | 报告各阶段成功率、失败归因、耗时及视觉误差 |

阶段 1 正在验收中，阶段 2–5 尚未开始；必须按顺序通过，不能跳过阶段 1 直接把当前独立 MoveIt 桥接当成完整任务。

阶段 1 的额外验收：关闭或降低 VNC viewer 帧率后，仿真步数、轨迹执行结果和任务状态机结果应保持一致；固定和腕部画面均能与对应感知帧时间戳匹配。阶段 5 的额外验收：每个 ROS 会话使用独立域，且加入视觉随机化后单独报告指标。

## 首个回合的判定

任务“抓取红色杯子并放到托盘”仅在同时满足以下条件时成功：

1. 语义模块选择的目标是红色杯子；
2. 几何模块由 RGB-D 估计出可用的目标位姿；
3. 状态机依次完成预抓取、接近、闭夹爪、抬升、放置和释放；
4. MuJoCo 接触/附着事件确认杯子曾被夹住；
5. 杯子最终位于托盘区域，且相机复检未发现错误杯子。

## 必存日志

每个回合保存在 `logs/episodes/<episode_id>/`，至少包括：文本指令、随机种子、相机 RGB/深度、VLM 原始输出与校验结果、几何估计、仅评测真值、状态机转移、MoveIt 轨迹、夹爪命令、MuJoCo 事件和最终判定。

## 指标

| 指标 | 计算方式 |
| --- | --- |
| 语义目标正确率 | VLM/语义目标与评测标签一致的回合占比 |
| 位姿可用率 | 满足置信度和工作空间约束的感知输出占比 |
| 平移/旋转误差 | 估计与真值的误差，仅作评测 |
| 规划成功率 | 有效技能目标中 MoveIt 成功规划的比例 |
| 抓取成功率 | 发生有效夹取附着的比例 |
| 放置成功率 | 杯子最终落在目标托盘区域的比例 |
| 端到端成功率 | 同时满足首个回合五项判定的比例 |
| 重试收益 | 重试后成功率相对首次尝试的提升 |
