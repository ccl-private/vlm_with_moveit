# MoveIt 视觉闭环服务器仿真实验：对齐方案

## 结论与范围

本实验的交付物不是“能让 Panda 移动一次”，而是一个在单一仿真任务世界中可重复运行的闭环：

```text
中文文本指令
  → VLM 目标语义与步骤判定
  → 模拟 RGB-D 相机 + 几何定位
  → 任务状态机
  → MoveIt 碰撞规划
  → MuJoCo Panda/夹爪执行
  → 相机与物理事件复检
  → 成功、重试或失败归因
```

首个验收任务固定为：**“抓取桌面随机位置的指定颜色杯子，并放到固定托盘。”** 文本指令例如“抓取红色杯子并放到托盘”。Panda 是参考机械臂；执行对象是本实验目录中的 MuJoCo Panda 假机器人，而非真机。

## 当前真实状态

| 已有能力 | 作用 | 不能代表什么 |
| --- | --- | --- |
| MuJoCo 彩色杯子 RGB-D 场景 | 验证相机、深度反投影与日志 | 没有 Panda 和夹爪，不是抓取仿真 |
| `ColorSemanticAdapter` | 验证文字到目标颜色的接口 | 不是 VLM |
| Panda + MoveIt + 假控制器 | 验证 IK、规划、RViz 与关节轨迹执行 | 与杯子 MuJoCo 场景尚未统一 |
| 手工发布 `/vision_moveit/target_pose` | 验证单次 6D 目标到关节执行 | 不是完整任务，不计入任务成功率 |

因此，后续实现不得把 RViz 中一次机械臂移动称为“抓取成功”。只有满足视觉输入、物体接触/附着、放置事件和相机复检的完整回合，才计为成功。

## 统一任务世界

下一代场景必须在同一个 MuJoCo XML 中包含以下实体：Panda 七轴臂、平行夹爪、桌面、红绿蓝杯子、托盘、固定 RGB-D 相机和腕部 RGB-D 相机。场景应提供两类数据：

- 正常控制路径：相机 RGB、深度、内外参、关节状态、夹爪状态和接触事件；
- 仅评测路径：物体/末端真值位姿及物理事件真值。

MoveIt 使用同一 Panda 模型，并将桌面、杯子、托盘同步为碰撞物。MuJoCo 是物理与传感器真源；MoveIt 只负责自由空间的运动学、碰撞规划与关节轨迹。

## VNC 观测布局

阶段 1 必须提供一个从 VNC 桌面启动的观测入口。运行一个回合时，操作者应能同时或可快速切换地看到以下四类信息：

| 视图 | 内容 | 数据来源 |
| --- | --- | --- |
| MuJoCo 全局视图 | Panda、夹爪、桌面、杯子和托盘的第三人称物理执行过程 | MuJoCo viewer |
| 固定虚拟相机视图 | 与主视角 VLM/几何感知实际读取的同一帧 RGB；可切换深度伪彩 | MuJoCo `camera_fixed` 渲染结果 |
| 腕部虚拟相机视图 | 与末端相机实际读取的同一帧 RGB；用于接近、抓取与放置复检 | MuJoCo `camera_wrist` 渲染结果 |
| RViz 视图 | MoveIt 碰撞场景、目标姿态和待执行/已执行关节轨迹 | ROS 2 / MoveIt |
| 任务状态视图 | 当前指令、识别目标、位姿置信度、状态机阶段、事件和失败原因 | 任务日志与状态机输出 |

VNC 只负责观测和调试，不能向控制路径注入物体真值。固定和腕部虚拟相机窗口的 RGB-D 必须与感知模块使用同一内存帧或同一带时间戳的帧缓存，以便画面、定位和日志一一对应。默认采用“全局 viewer + 右上固定相机 + 右下腕部相机”的单窗口叠加布局；侧视角只作为可选调试视图。

## 模块职责

| 模块 | 负责 | 明确不负责 |
| --- | --- | --- |
| VLM 适配器 | 读取文本、RGB 与任务上下文，选择目标类别/实例、给出语义置信度和标准化任务意图 | 输出毫米级 6D 位姿、关节命令或替代安全逻辑 |
| 几何感知 | 利用目标语义掩码/类别、深度和标定求目标 6D 位姿及抓取候选 | 决定任务流程 |
| 任务状态机 | `观测→预抓取→接近→闭夹爪→附着确认→抬升→放置→复检`，管理重试与停止 | 直接生成高频控制量 |
| MoveIt | IK、碰撞场景、分段轨迹与失败原因 | 图像理解、接触动力学 |
| MuJoCo 执行适配器 | 回放关节轨迹与夹爪命令，输出关节/接触/物体状态 | 路径规划或语义判断 |
| VNC 可视化 | 同时观察 MuJoCo 相机/执行画面、RViz 规划与阶段状态 | 作为控制输入或评测真值 |

## VLM 策略

第一版保留 `ColorSemanticAdapter` 作为无模型对照组，并新增可替换的本地 VLM 适配器。VLM 模型文件、权重缓存和推理代码均必须位于 `moveit_experiment/`；不得依赖父项目的模型目录。VLM 的输出必须经过 JSON 结构校验和置信度阈值检查，再交给几何模块。

首个 VLM 能力只需回答“指令要求哪个杯子/托盘、任务是否完成或发生何种异常”。即使 VLM 给出抓取建议，最终抓取姿态仍由深度几何和预定义夹爪约束生成。

## 设计审查结论与硬约束

以下约束是在实施阶段 1 前确认的，优先级高于任何局部演示便利性：

| 风险 | 强制设计决策 |
| --- | --- |
| 多套仿真世界不一致 | 阶段 1 后的任务执行只能使用统一 MuJoCo 场景。现有 Panda Fake 控制器只保留为 MoveIt 冒烟测试，不能与 MuJoCo Panda 并行执行同一回合。 |
| MuJoCo 与 MoveIt 场景漂移 | 建立本目录内唯一的场景配置来源；每次技能规划前同步 Panda 关节状态、桌面、杯子和托盘碰撞物到 MoveIt。 |
| 仅轨迹成功却抓取失败 | 夹爪接触条件满足后，MuJoCo 的物理附着状态与 MoveIt 的附着碰撞物必须作为同一个状态转换同步建立；释放时同步解除。 |
| VNC 影响控制时序 | 物理仿真、轨迹执行、VLM 推理、相机渲染和 VNC viewer 必须解耦。viewer 只按显示帧率刷新，不能充当仿真或控制时钟。 |
| VLM 输出不可靠 | VLM 只输出经过 JSON 校验的语义意图；必须有超时、置信度拒绝和颜色语义基线回退，不允许产生关节命令。 |
| 模拟视觉过于理想 | 评测阶段加入光照、材质、遮挡、深度噪声和帧延迟随机化；真值始终与控制路径隔离。 |
| 同机 ROS 会话串话 | 每个回合/会话使用显式的独立 `ROS_DOMAIN_ID` 和独立日志目录；不得依赖一个永久固定的默认域。 |

## 运行频率与同步点

物理仿真以固定步长运行；MoveIt 只在状态机进入“需要新技能轨迹”的阶段规划；VLM 按需异步推理；固定/腕部相机帧和 VNC viewer 以较低独立帧率刷新。所有进入规划器的目标物体位姿、相机帧和关节状态必须带回合编号与时间戳，状态机只能使用同一观测快照或明确的时间对齐结果。

## 安全与真值规则

- `execute_in_simulation=true` 只能由 MuJoCo Panda 假执行器启动脚本设置；其默认值必须为关闭。
- 真机适配器未来必须另建，并要求显式授权、速度限制、急停与安全区域；不得复用仿真自动执行开关。
- MuJoCo 物体真值不得进入正式感知→规划控制路径；仅允许用于单元测试、标注和指标计算。
- 每一次任务回合必须保存输入图像、感知估计、真值评测、状态转移、规划/执行结果和复检结果。
